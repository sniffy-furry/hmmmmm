#pragma once
// ─── ui/Toast.h — REMOVED (VanGUI Overhaul Phase 3) ──────────────────────────
// The hand-rolled ToastManager has been replaced by vangui_notify.
// Use VanGui::NotifyInfo / NotifySuccess / NotifyWarning / NotifyError instead
// (see vendor/VanGUI/misc/vangui_notify.h). Not part of the build; do not include.
#error "ui/Toast.h is removed — use <vangui_notify.h> (VanGui::Notify*) instead."
